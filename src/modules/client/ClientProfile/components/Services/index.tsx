import { Block } from '@UI';
import styles from './Services.module.scss';

export const Services = () => {
  return (
    <Block className={styles.block}>
      <h2>УСЛУГИ</h2>
      <span>0 подключено</span>
    </Block>
  );
};
